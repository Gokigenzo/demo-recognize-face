# Supabase credentials — required for cloud persistence.
# If these are not set, the application runs in offline/local mode.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-or-anon-key-here
